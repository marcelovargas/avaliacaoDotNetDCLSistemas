@model Avaliacao.Data.Models.CarSale

@{
    ViewData["Title"] = "Mostrar cadastro de venda de carro";
}

<h1 class="text-primary">@ViewData["Title"]</h1>

<div>

    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Price)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Price)
        </dd>
    </dl>
</div>

<div >
    <a asp-action="Edit" class="btn btn-outline-warning" asp-route-id="@Model.Id">Edit</a>
    <a asp-action="Index" class="btn btn-outline-success">Volver</a>
</div>
